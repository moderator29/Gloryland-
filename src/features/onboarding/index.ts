/**
 * Onboarding: the two moments that introduce the product.
 *
 * Aperture is the opening reveal, once per session. Orientation is the banner
 * that points at the first run route and disappears for good once the member
 * has either finished it or opened a vault.
 */

export { Orientation, ORIENTATION_KEY, ORIENTATION_DISMISS_KEY } from "./Orientation";
export type { OrientationProps } from "./Orientation";
export { Aperture, APERTURE_KEY } from "./Aperture";
export type { ApertureProps } from "./Aperture";
